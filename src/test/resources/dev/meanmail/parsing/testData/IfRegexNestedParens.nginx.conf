# https://github.com/meanmail-dev/nginx-intellij-plugin/issues/40
location / {
    if ($args ~ [\&\?]stream.*?=(.*)) {
        return 403;
    }

    if ($http_referer ~* (baidu|soso|youdao)) {
        return 403;
    }

    proxy_pass http://backend;
}
