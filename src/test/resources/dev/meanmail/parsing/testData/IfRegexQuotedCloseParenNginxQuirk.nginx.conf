# https://github.com/meanmail-dev/nginx-intellij-plugin/issues/77
# Case 2: nginx parser quirk - ) inside quotes closes if()
# nginx accepts this: regex becomes "x, then { opens block
server {
    if ($arg_a ~ "x)"{
        set $a 1;
    }
}
