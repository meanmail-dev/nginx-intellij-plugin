# https://github.com/meanmail-dev/nginx-intellij-plugin/issues/77
# Case 5: Unquoted regex with unmatched closing paren
# nginx: pcre2_compile() failed: unmatched closing parenthesis in "x)"
server {
    if ($arg_a ~ x)) {
        set $a 1;
    }
}
