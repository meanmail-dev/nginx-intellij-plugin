server {
    listen 80;

    location / {
        try_files $uri /index.php?=;
    }

    location /app {
        try_files $uri /index.php?key= /fallback;
    }

    location /multi {
        try_files $uri /index.php?a=1&b= /fallback;
    }
}
