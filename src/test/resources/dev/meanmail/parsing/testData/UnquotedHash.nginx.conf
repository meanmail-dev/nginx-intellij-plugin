server {
    listen 80;

    if ($request_uri ~* (?:#.*#)) {
        return 403;
    }

    set $x value#test;

    rewrite ^/(?:#test) /new last;

    add_header X-Test value#here;
}

map $uri $result {
    ~*#test 1;
    default 0;
}
